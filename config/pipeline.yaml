
project:
  name: "amazon_review_summarization"
  seed: 42

stages:
  - name: sft
    config: "config/sft.yaml"
    output_dir: "models/sft"
    resume: false

  - name: dpo
    config: "config/dpo.yaml"
    output_dir: "models/dpo"
    resume: false

  - name: rlhf
    config: "config/rlhf.yaml"
    output_dir: "models/rlhf"
    resume: false
